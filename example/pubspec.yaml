name: network_usage_monitor_example
description: Demonstrates how to use the network_usage_monitor plugin.
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  network_usage_monitor:
    path: ../
  dio: ^5.9.2
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
